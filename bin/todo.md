detect monorepos (if current directory does not have a lockfile but somewhere above in the fs tree there is one)
use package.json if it is a monorepo

impl parsing for following package managers
- deno
- yarn
- pnpm
